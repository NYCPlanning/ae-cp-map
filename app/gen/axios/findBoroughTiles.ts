/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  FindBoroughTilesQueryResponse,
  FindBoroughTilesPathParams,
  FindBoroughTiles400,
  FindBoroughTiles500,
} from "../types/FindBoroughTiles.js";
import type {
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getFindBoroughTilesUrl(
  z: FindBoroughTilesPathParams["z"],
  x: FindBoroughTilesPathParams["x"],
  y: FindBoroughTilesPathParams["y"],
) {
  const res = { method: "GET", url: `/boroughs/${z}/${x}/${y}.pbf` as const };
  return res;
}

/**
 * @summary Mapbox Vector Tiles for boroughs
 * {@link /boroughs/:z/:x/:y.pbf}
 */
export async function findBoroughTiles(
  z: FindBoroughTilesPathParams["z"],
  x: FindBoroughTilesPathParams["x"],
  y: FindBoroughTilesPathParams["y"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    FindBoroughTilesQueryResponse,
    ResponseErrorConfig<FindBoroughTiles400 | FindBoroughTiles500>,
    unknown
  >({
    method: "GET",
    url: getFindBoroughTilesUrl(z, x, y).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
